# Python dependencies for the YOLOMG continual-learning experiments.
#
# Versions below reflect the Snellius HPC environment (conda env: uav_master,
# CUDA 12.1, A100/H100 nodes). Exact pinned versions can be obtained by running
# `conda list` inside the uav_master environment on Snellius.
#
# The YOLOMG detector (YOLOMG-main/) has its own internal imports (models/,
# utils/, FD5_mask) that are resolved via sys.path and do not need to be
# installed separately.

# Deep learning
torch>=2.1.0
torchvision>=0.16.0

# Computer vision
opencv-python>=4.8.0
Pillow>=10.0.0

# Numerical / scientific
numpy>=1.24.0
scipy>=1.11.0

# Data manipulation
pandas>=2.0.0

# Plotting
matplotlib>=3.7.0

# Utilities
tqdm>=4.66.0
PyYAML>=6.0
